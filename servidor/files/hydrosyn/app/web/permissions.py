from fastapi import APIRouter, Request
router = APIRouter()

# consultar permisos  usuarios o sistema ¿ que usuarios tienen permiso o rl x en o en que sistema?
#
# cerar roles 
# consultar rles ¿ roles existen, que roles con x permisos?
# modificar borra roels
